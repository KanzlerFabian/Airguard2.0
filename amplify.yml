version: 1
frontend:
  phases:
    build:
      commands:
        - cd airguard-web
        - npm run build
  artifacts:
    baseDirectory: airguard-web/public
    files:
      - '**/*'
  cache:
    paths: []
